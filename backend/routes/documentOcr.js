// v0 scaffold for: Document OCR + NLP extraction
// Generated by batch_00 audit. Wire LLM, vendor APIs, or DB models as needed.
const express = require('express');
const router = express.Router();
const { authenticateToken: auth } = require('../middleware/auth');

router.use(auth);

// GET /document-ocr/health — readiness probe
router.get('/health', (req, res) => {
  res.json({ feature: 'document-ocr', status: 'ok', version: 'v0' });
});

// POST /document-ocr/run — main feature handler
router.post('/run', async (req, res) => {
  try {
    const input = req.body || {};
    // TODO: wire LLM client (OpenRouter / OpenAI) or vendor integration
    const result = {
      feature: 'document-ocr',
      receivedKeys: Object.keys(input),
      stage: 'scaffold',
      note: 'Implementation pending. See feature suggestion: Document OCR + NLP extraction',
    };
    return res.json({ success: true, result });
  } catch (err) {
    console.error('[document-ocr] error', err);
    return res.status(500).json({ success: false, error: err.message });
  }
});

// GET /document-ocr/history — list recent runs (stub)
router.get('/history', async (req, res) => {
  // TODO: persist + query from DB. Returning empty list for v0.
  res.json({ items: [], note: 'Persistence not wired in v0' });
});

module.exports = router;
